{
	"name": "revenue-auditor",
	"version": "1.0.2",
	"description": "Obsidian plugin that runs a mock AI revenue audit on the active note.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck -p tsconfig.json && node esbuild.config.mjs production"
	},
	"keywords": [
		"obsidian-plugin"
	],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.19.11",
		"obsidian": "latest",
		"typescript": "4.7.4"
	},
	"dependencies": {
		"pdf-parse": "^1.1.4"
	}
}
